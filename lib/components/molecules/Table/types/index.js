// @flow
import type { Node } from 'react';

export type Props = {
  className?: string,
  data: Array<mixed>,
  children: Node,
  vertical?: boolean,
  dataMappingKey: mixed,
};

export type HeadProps = {
  children: node,
};
