// @flow
import type { Element, Node } from 'react';

export type Props = {
  tootTipContent?: string,
  showImage?: boolean,
  domElement?: Node,
  className?: string,
};

export type Div = Element<'div'>;
