const defaultConfig = {
  className: 'test-line',
  height: '20px',
  width: '100%',
  marginBottom: '0',
};

const circleConfig = {
  className: 'test-circle',
  width: '100px',
  marginBottom: '25px',
};

const accordionConfig = {
  className: 'accordion',
  height: '50px',
  width: '100%',
  marginBottom: '2px',
};

const tabsConfig = {
  className: 'tabs',
  height: '46px',
  width: '84px',
  marginBottom: '1px',
};

const tabsContentConfig = {
  className: 'tabs',
  height: '84px',
  width: '936px',
  marginBottom: '10px',
};

export { defaultConfig, circleConfig, accordionConfig, tabsConfig, tabsContentConfig };
