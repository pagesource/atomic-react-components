import { css } from 'styled-components';

const styles = css`
  margin-bottom: 0.75rem; //TODO: Fix the duplicate styling in input.style.js

  label {
    display: block;
    margin-bottom: 0.25rem;
  }

  input {
    margin-bottom: 0.25rem;
  }
`;

export default styles;
