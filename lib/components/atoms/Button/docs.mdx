// @flow
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { Fragment } from 'react';
import ButtonVanilla from './index.js';
import { primaryButton } from './Button.mock';

<Meta title="Button" component={ButtonVanilla} />

# Button #

Button communicate actions a user can take  to perform tasks.

# Props #

<Props of={ButtonVanilla} />

<Preview>
  <Story name="Primary">
  <Fragment>
    <ButtonVanilla {...primaryButton}>Submit</ButtonVanilla>
  </Fragment>
  </Story>
</Preview>

