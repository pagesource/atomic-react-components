import { Meta, Story, Preview, Props  } from '@storybook/addon-docs/blocks';
import Video, { VideoVanilla } from '.';
import { defaultConfig, vanillaDefaultConfig } from './Video.mock';

<Meta title="Video" component={VideoVanilla} />


### Video Knobs
<Preview>
  <Story name="Video Knobs">
    <VideoVanilla {...vanillaDefaultConfig}>
      <Video.track {...vanillaDefaultConfig.trackdefaultdata} />
    </VideoVanilla>
  </Story>
</Preview>

### Video
<Preview>
  <Story name="Default Video">
<Video {...defaultConfig}>
      {defaultConfig.trackdata.map(caption => (
        <Video.track
          src={caption.src}
          default={caption.isDefault}
          kind="subtitles"
          srcLang={caption.srcLang}
          label={caption.label}
          key={caption.id}
        />
      ))}
    </Video>
  </Story>
</Preview>


## Props 🔧

<Props of={VideoVanilla} />
